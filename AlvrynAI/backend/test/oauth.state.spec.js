const { expect } = require('chai');

describe('oauth.state.spec (legacy)', function(){
  it('placeholder test - legacy passport-based tests were removed in favor of server-side OAuth', function(){
    expect(true).to.equal(true);
  });
});
